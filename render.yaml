services:
  - type: worker
    name: candidate-support-automation
    env: node
    buildCommand: npm install
    startCommand: node main-continuous.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OTOMEYT_LOGO_URL
        sync: false
        optional: true

